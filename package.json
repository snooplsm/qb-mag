{
  "name": "qbittorrent-magnet-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --bundles app && ./scripts/build-dmg.sh",
    "install:mac": "./scripts/install-macos.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
